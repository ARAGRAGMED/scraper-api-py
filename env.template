# Environment Variables for Scraper API
# Copy this file to .env and fill in your values

# Proxy Configuration
# Set USE_PROXY=true to enable proxy usage
USE_PROXY=false

# Proxy server details (if using custom proxy)
PROXY_URL=your-proxy-server.com:8080
PROXY_TYPE=http  # http, https, socks5
PROXY_USERNAME=your-username
PROXY_PASSWORD=your-password
PROXY_TIMEOUT=30

# Fallback proxy timeout (in seconds) - reduced for faster failure
FALLBACK_TIMEOUT=8

# Example proxy configurations:

# Example 1: HTTP Proxy
# USE_PROXY=true
# PROXY_URL=proxy.example.com:8080
# PROXY_TYPE=http
# PROXY_USERNAME=user123
# PROXY_PASSWORD=pass123

# Example 2: SOCKS5 Proxy
# USE_PROXY=true
# PROXY_URL=proxy.example.com:1080
# PROXY_TYPE=socks5
# PROXY_USERNAME=user123
# PROXY_PASSWORD=pass123

# Example 3: No custom proxy (uses fallback proxies)
# USE_PROXY=false
# (fallback proxies will be used automatically)

# Note: If no custom proxy is configured, the system will automatically
# use fallback proxy services to ensure your IP is never exposed.
